{{#welp-layout as |authenticated themeName logout|}}
  {{#if authenticated}}
    {{welp-header class=themeName logout=logout}}
    {{outlet}}
  {{else}}
    {{#welp-login as |username login|}}
      {{login-form username=username login=login}}
    {{/welp-login}}
  {{/if}}
{{/welp-layout}}
